<!DOCTYPE HTML>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <title>Let's tank some fish!</title>
    <link rel="stylesheet" type="text/css" href="http://fonts.googleapis.com/css?family=Tangerine">
    <%= stylesheet_link_tag "jquery-ui-1.8.11.custom", "vtip", "style" %>
    <%= javascript_include_tag "jquery-1.5.1.min", "jquery-ui-1.8.11.custom.min", "vtip-min", "application" %>
  </head>
  
  <body>
    <%# debug @species %>
    <div id="wrapper">
      <div id="logo">
        <%= image_tag("logo.png") %>
      </div>
      <div class="wizard">
        <div id="step1">
          <div class="video">
            <iframe src="http://player.vimeo.com/video/14109777" width="640" height="360" frameborder="0"></iframe>
          </div>
          <div class="text">
            <div>Set your tank size in </div><input name="volume" type="text" value="liters" onfocus="if($(this).val()=='liters') {$(this).val('');}" onblur=" if($(this).val().length==0) { $(this).val('liters'); }"> <div>and</div><a href="#" class="orangeButton" onclick="createAquarium()">GET STARTED &raquo;</a>
          </div>
        </div>
        
        <div id="step2" style="display:none">
          <div class="search">
            <span>Search for fish</span>
            <input id="search" name="search" type="text" value="Enter keywords..." onfocus="if($(this).val()=='Enter keywords...') {$(this).val('');}" onblur=" if($(this).val().length=='') { $(this).val('Enter keywords...'); }"> 
          </div>
          
          <div id="category_list">
            <div class="top"></div>
              <ul>
                <% @families.each do |f| -%>
                  <li><%= link_to f.common_name, '#', :onclick => "doFamilySearch(" + f.id.to_s + ");" -%></li>
                <% end %>
              </ul>
            <div class="bottom"></div>
          </div>
          <ul id="search_result">
            <% @species.each do |s| %>
              <li id="fish_<%= s.id.to_s%>">
                <%= image_tag(s.photo.url('thumb'), :class => 'vtip', :title => s.common_name, :width => '150px', :height => '113px', :onClick => 'moreInfo("#fish_'+ s.id.to_s+'")') %>
                <div style="display:none">
                  <div class="name"><%= s.common_name %></div>
                  <div class="description"><%= simple_format(s.description) %></div>
                  <div class="image"><%= image_tag(s.photo.url('medium', :width => "400px")) %></div>
                  <div class="min_group_size"><%= s.min_group_size %></div>
                </div>
              </li>
            <% end %>
          </ul>
          <div class="button back" onclick="prevStep('step1')">
           &laquo; Back
          </div>
          <div class="button next" onclick="nextStep('step3')">
            I'm done show me my tank! &raquo;
          </div>
          
        </div>
        
        <div id="step3" style="display:none">
          <h1>Great job tanking your fish!</h1>
          <ul id="selected_fishes">
            <% @species.each do |s| %>
              <li id="fish_<%= s.id.to_s%>">
                <%= image_tag(s.photo.url('thumb'), :class => 'vtip', :title => s.common_name,  :width => '150px', :height => '113px', :onClick => 'moreInfo("#fish_'+ s.id.to_s+'")') %>
                <div style="display:none">
                  <div class="name"><%= s.common_name %></div>
                  <div class="description"><%= simple_format(s.description) %></div>
                  <div class="image"><%= image_tag(s.photo.url('medium', :width => "400px")) %></div>
                  <div class="min_group_size"><%= s.min_group_size %></div>
                </div>
              </li>
            <% end %>
          
          </ul>
          <div id="tank_properties">
            <h1>My fishtank</h1>
            <ul>
              <li>Volume: <strong>90L</strong></li>
              <li>Bioload:</li>
              <li>Temp: <strong>18-23C</strong></li>
              <li>pH: 5-7 | dH 3-17</li>
            </ul>
            <div class="text">
              Your selected fish are compatible for your tank.<br><br>
              Your selected fish are not compatible.
            </div>
          </div>
          <div class="button back" onclick="prevStep('step2')">
           &laquo; Back
          </div>
        </div>
      </div>
      
      <!-- /wizard -->
      
      <div id="footer">
        <div id="account">
          <% if current_user %>
            <%= truncate(current_user.name) %>
            <%= link_to 'Sign out', destroy_user_session_path %>
          <% else %>
            <%= link_to_rpx 'Sign up', user_session_url %>
            <%= link_to_rpx 'Sign in', user_session_url %>
          <% end %>
          <%= javascript_include_rpx(user_session_url) %>
        </div>
      </div>
      
    </div>
    <!-- /wrapper -->

      <div id="dialog" style="display:none">
        <div class="left_panel">
          <div class="image"><%= image_tag('nemo.jpg') %></div>
          <div class="options">
            <input type="text" name="fish_counter" value="2">
            <div class="add_fish_btn" onclick="addFish()">Add to tank</div>
          </div>
        </div>
        <div class="right_panel">
          <h1></h1>
          <div class="info"></div>
        </div>
      </div>

  </body>
  
</html> 


